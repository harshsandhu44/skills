## What this changes

## Checklist

- [ ] `claude plugin validate . --strict` passes
- [ ] `claude plugin validate plugins/skills --strict` passes
- [ ] `npx markdownlint-cli2 "**/*.md"` is clean
- [ ] CHANGELOG / version bumped (if releasing)
