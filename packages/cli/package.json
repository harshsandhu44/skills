{
  "name": "@harshsandhu44/skills-cli",
  "version": "1.1.0",
  "description": "Install Claude Code skills from the community skills library",
  "bin": {
    "claude-skills": "./dist/index.js"
  },
  "type": "module",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "bun build src/index.ts --outfile dist/index.js --target node --minify --banner '#!/usr/bin/env node' && chmod +x dist/index.js",
    "dev": "bun run src/index.ts",
    "lint": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "bun-types": "^1.3.11",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18"
  },
  "publishConfig": {
    "access": "public",
    "registry": "https://npm.pkg.github.com"
  }
}
